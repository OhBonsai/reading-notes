#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

#define MAX_WORD 20

int main(void) {
	char a[10], b[10];
	printf("Enter a first and last name: ");
	scanf("%s %s", a, b);

	printf("Enter second\n");

	printf("a is %s", a);
	printf("b is %s", b);



	return 0;
}
